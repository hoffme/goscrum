import styled from "styled-components";

const ContainerFilterStyle = styled.form`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  padding: 0.25rem;

  > * {
    margin: 0.25rem;
    flex: 1;
  }
`;

export {
    ContainerFilterStyle
}